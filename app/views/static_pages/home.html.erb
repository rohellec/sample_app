<!--partials moved to shared folder since page can be rendered without http request
in which case 'render' needs full path from the 'view' folder--> 
<% if signed_in? %>
  <%= render "shared/home_logged_in" %>
<% else %>
  <%= render "shared/home_static" %>
<% end %>
